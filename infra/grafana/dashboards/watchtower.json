{
  "annotations": {"list": []},
  "panels": [
    {
      "type": "timeseries",
      "title": "Check Latency (p95)",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(watchtower_check_latency_seconds_bucket[5m])) by (le))",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Check Failures",
      "targets": [
        {
          "expr": "sum(rate(watchtower_check_requests_total{status=\"fail\"}[5m]))",
          "refId": "B"
        }
      ]
    }
  ],
  "schemaVersion": 39,
  "title": "Watchtower Overview",
  "version": 1
}
